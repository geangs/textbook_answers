<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.textbookanswers">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TextbookAnswers">
        <activity android:name=".AddAnswer" />
        <activity
            android:name=".SearchResultActivity"
            android:exported="false"
            android:label="@string/title_activity_search_result"
            android:theme="@style/Theme.TextbookAnswers.NoActionBar" />
        <activity
            android:name=".SignIn"
            android:exported="false" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AnswerActivity" />
        <activity android:name=".SectionActivity" />
        <activity android:name=".BookTitleDialog" />
        <activity android:name=".ExercisesActivity" />
        <activity android:name=".ChaptersActivity" />
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>